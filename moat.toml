# This repo never uses pull requests, so the "require a pull request" rule is dropped here.
# Both Moat checks are silenced on purpose; signed-commit, force-push and deletion protection
# all remain enforced.
[checks]
repositories_pull_requests_require_reviews = "off"
repositories_workflow_permissions_are_restricted = "off"
